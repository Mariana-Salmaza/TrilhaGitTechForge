// 1.Declare duas variáveis, a e b, e atribua valores numéricos a elas. Crie uma terceira variável, soma, e atribua a soma de a e b a ela. Imprima o resultado no console.

// Declaração e atribuição de valores às variáveis 'a' e 'b'
let a = 10;
let b = 20;

// Criação de uma terceira variável 'soma' e atribuição da soma de 'a' e 'b' a ela
let soma = a + b;

// resultado no console
console.log("A soma de", a, "e", b, "é igual a", soma);
